<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <style>
        /* sidebar.css */
        :root {
            --primary: #2563eb;
            --secondary: #64748b;
            --sidebar-width: 250px;
            --sidebar-bg: white;
            --sidebar-hover: #f8fafc;
        }

        .sidebar {
            position: fixed;
            width: var(--sidebar-width);
            height: 100vh;
            background: var(--sidebar-bg);
            padding: 1rem;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.1);
            top: 0; /* Collé en haut */
            left: 0; /* Collé à gauche */
        }

        .sidebar .logo {
            font-size: 1.5rem;
            font-weight: bold;
            color: var(--primary);
            margin-bottom: 2rem;
            padding: 1rem;
            display: flex;
            align-items: center;
            gap: 0.75rem;
        }

        .sidebar .nav-item {
            display: flex;
            align-items: center;
            padding: 0.75rem 1rem;
            color: var(--secondary);
            text-decoration: none;
            border-radius: 0.5rem;
            margin-bottom: 0.5rem;
            transition: all 0.3s;
        }

        .sidebar .nav-item:hover,
        .sidebar .nav-item.active {
            background: var(--sidebar-hover);
            color: var(--primary);
        }

        .sidebar .nav-item i {
            margin-right: 0.75rem;
            width: 20px;
        }

        /* Ajustement du contenu principal */
        .main-content {
            margin-left: var(--sidebar-width);
            padding: 2rem;
        }

        /* Media queries pour la responsivité */
        @media (max-width: 768px) {
            .sidebar {
                transform: translateX(-100%);
                transition: transform 0.3s ease;
                z-index: 1000;
            }

            .sidebar.active {
                transform: translateX(0);
            }

            .main-content {
                margin-left: 0;
            }
        }
    </style>
</head>
<body>
    <nav class="sidebar">
        <div class="logo">
            <i class="fas fa-hospital"></i> MediGest
        </div>
        <a href="/dashboard" class="nav-item <%= currentPage === 'dashboard' ? 'active' : '' %>">
            <i class="fas fa-home"></i>
            Tableau de bord
        </a>
        <a href="/dashboard/patients" class="nav-item <%= currentPage === 'patients' ? 'active' : '' %>">
            <i class="fas fa-user-injured"></i>
            Patients
        </a>
        <a href="/dashboard/appointments" class="nav-item <%= currentPage === 'appointments' ? 'active' : '' %>">
            <i class="fas fa-calendar-alt"></i>
            Rendez-vous
        </a>
        <a href="/dashboard/staff" class="nav-item <%= currentPage === 'staff' ? 'active' : '' %>">
            <i class="fas fa-user-md"></i>
            Personnel
        </a>
        <a href="/dashboard/stats" class="nav-item <%= currentPage === 'stats' ? 'active' : '' %>">
            <i class="fas fa-chart-bar"></i>
            Statistiques
        </a>
        <a href="/dashboard/settings" class="nav-item <%= currentPage === 'settings' ? 'active' : '' %>">
            <i class="fas fa-cog"></i>
            Paramètres
        </a>
        <a href="/logout" class="nav-item">
            <i class="fas fa-sign-out-alt"></i>
            Déconnexion
        </a>
    </nav>
</body>
</html>